apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-doctor-service-deployment
  labels:
    app: ts-doctor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ts-doctor-service
  template:
    metadata:
      labels:
        app: ts-doctor-service
    spec:
      containers:
        - name: ts-doctor-service
          image: subhankarm2312/ts-doctor-service # CORRECTED: Placeholder name now matches ci.yml
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms128m -Xmx256m"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef:
                name: ts-doctor-service-config
            - secretRef:
                name: ts-doctor-service-secret # CORRECTED: Name now matches secret.yml
